apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "msa-broadcaster.fullname" . }}
  labels:
    app: {{ include "msa-broadcaster.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "msa-broadcaster.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "msa-broadcaster.name" . }}
    spec:
      containers:
        - name: {{ include "msa-broadcaster.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
      imagePullSecrets:
        - name: {{ .Values.image.pullSecretName }}
